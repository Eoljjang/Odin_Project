let myLibrary = [];

function book(title, author, length) {
    this.title = title;
    this.author = author;
    this.length = length;
}

function addBookToLibrary(book){
    ;
}

function main(){
    b1 = book("Monopoly", "MLK", 2000);
    b2 = book("Stratego", "Steve Jobs", 802);
}